{
  "name": "ace",
  "private": true,
  "description": "Root package for Vercel — Next.js app lives in ./ace-site",
  "dependencies": {
    "next": "14.2.33",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "scripts": {
    "build": "npm run build --prefix ace-site",
    "dev": "npm run dev --prefix ace-site",
    "start": "npm run start --prefix ace-site",
    "lint": "npm run lint --prefix ace-site"
  }
}
